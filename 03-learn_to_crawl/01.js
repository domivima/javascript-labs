// Tidy up the expression below so that all unnecessary whitespace is
// eliminated. The the sum should be displayed on the console. Debug and modify
// the code so that the console does not display any errors.


let x = 78347823;
let y = 843972;
result = x * y;
console.log(result);
